include $(APPDIR)/Make.defs

PROGNAME  = $(CONFIG_VAPOROS_PORTABLE_CAT_PROGNAME)
PRIORITY  = $(CONFIG_VAPOROS_PORTABLE_CAT_PRIORITY)
STACKSIZE = $(CONFIG_VAPOROS_PORTABLE_CAT_STACKSIZE)
MODULE    = $(CONFIG_VAPOROS_PORTABLE_CAT)

MAINSRC = portable_cat_main.c

include $(APPDIR)/external/vapor-nostdinc.mk
include $(APPDIR)/Application.mk
